<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/button_white"
    tools:context=".InicioForm">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/formExplanation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="100dp"
            android:fontFamily="@font/inter"
            android:text="A continuación, deberá contestar a una serie de preguntas acerca de la experiencia en curso"
            android:textAlignment="center"
            android:textColor="@color/black"
            android:textSize="40dp"
            />
        <Button
            android:id="@+id/buttonStart"
            android:layout_width="400dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="80dp"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/button_greensabora"
            android:fontFamily="@font/inter"
            android:gravity="center"
            android:text="Comenzar cuestionario"
            android:textColor="@color/black"
            android:textAllCaps="false"
            android:textSize="30dp"
            android:paddingVertical="20dp"
            android:paddingHorizontal="70dp"
            />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>